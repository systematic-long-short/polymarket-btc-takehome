# polybench — pre-vetted dependency allowlist.
# Candidates may import ANY package listed here plus Python stdlib.
# Candidates may NOT ship their own requirements.txt — the security scanner
# rejects any import not on this list.
#
# Install with:   pip install -r requirements.txt
# Heavy install (torch + tensorflow CPU wheels ≈ 1–2 GB). Use a dedicated venv.

# --- Harness internals ---
httpx>=0.27,<1
websockets>=14,<16
pyarrow>=14,<19
pytest>=7.4,<9
pytest-asyncio>=0.23,<1

# --- Data & numerical ---
numpy>=1.26,<3
pandas>=2.1,<3
polars>=1.0,<2
scipy>=1.11,<2
statsmodels>=0.14,<1

# --- Classical ML ---
scikit-learn>=1.4,<2
lightgbm>=4.3,<5
xgboost>=2.0,<4
optuna>=3.5,<5

# --- Deep learning (CPU) ---
# torch and tensorflow ship CPU-capable wheels by default on PyPI. Candidates
# should not assume GPU at scoring time.
torch>=2.2,<3
tensorflow>=2.16,<3

# --- Technical indicators ---
ta>=0.11,<1
